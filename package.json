{
  "name": "@gliff-ai/document",
  "version": "0.1.0",
  "description": "gliff.ai DOCUMENT",
  "repository": "git://github.com/gliff-ai/document.git",
  "scripts": {
    "format": "npx --no-install prettier --write *.md",
    "prettier": "npx --no-install prettier -c *.md",
    "lint": "npm run lint:md && npm run prettier",
    "lint:md": "npx markdownlint *.md"
  },
  "license": "AGPL-3.0-only",
  "devDependencies": {
    "markdownlint-cli": "^0.28.1",
    "prettier": "^2.3.2"
  }
}
